git config --global user.email "당신의이메일@example.com"
git config --global user.name "당신의이름"
(맨처음에 한번만하면됨)

한지훈
git clone 깃허브주소
cd 깃허브에있는 폴더주소
git checkout -b 브런치이름

수정작업~~~

git add .
git commit -m "커밋이름(임의)"
git push -u origin 위에서설정한브런치이름
한지훈dadsasdadadaadad